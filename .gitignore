# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
dist-test/
build/
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Local pi.md created while developing the extension itself
# (this only ignores it inside THIS repo — projects that use the extension
# have their own pi.md handled by the extension at runtime)
pi.md
pi.md.local
.punched-memory.json

# Test / scratch
/tmp/
*.tmp
.env
.env.local

# Credentials & secrets — defense in depth (nothing committed today, guard against accidents)
.npmrc
secrets.json
*.pem
*.key
id_rsa*
*.p12
*.p8
credentials*.json

# punched-memory — never commit project-local memory
.punched-memory/
# t-plan: plan files are private runtime state — never commit or publish them
plan_*_[0-9a-zA-Z]*.md
plan.md
